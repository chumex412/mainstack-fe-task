import { Constants } from "@/application/domain/entities/general";

export const KEY_CODES: Constants["keyboardKeys"] = {
  ENTER: 13 || "Enter",
  SPACE: 32 || " ",
  DOWN_ARROW: 40 || "ArrowDown",
  UP: 38 || "ArrowUp",
  ESC: 27 || "Escape"
};
